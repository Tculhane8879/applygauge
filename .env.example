# Local PostgreSQL container settings. These development-only values are safe to
# commit, but should not be reused for a deployed environment.
POSTGRES_DB=applygauge
POSTGRES_USER=applygauge
POSTGRES_PASSWORD=applygauge_local
POSTGRES_PORT=5432
